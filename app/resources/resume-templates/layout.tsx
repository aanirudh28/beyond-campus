export const metadata = {
  title: 'Resume Templates — LSE, IIM, DU Format | Beyond Campus',
  description: '6 ATS-optimized resume templates for non-tech students — LSE format (free), IIM format, DU format, Startup format, Finance format, and Marketing format.',
  alternates: { canonical: 'https://www.beyond-campus.in/resources/resume-templates' },
}

export default function ResumeTemplatesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
